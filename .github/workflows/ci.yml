name: CI

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build & Push imagem Docker
    uses: suportess/suportes-workflow/.github/workflows/docker-build.yml@main
    with:
      app_name: app-suportes-frontend
      dockerfile_path: Dockerfile
      context: .
      platforms: linux/amd64
      build_args: |
        NEXT_PUBLIC_API_URL=${{ vars.NEXT_PUBLIC_API_URL }}
    secrets:
      DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}

  deploy:
    needs: build
    uses: suportess/suportes-workflow/.github/workflows/docker-deploy.yml@main
    with:
      app_name: app-suportes-frontend
      deploy_path: /opt/apps/app-suportes-frontend
    secrets:
      DOCKER_TOKEN: ${{ secrets.DOCKER_TOKEN }}
      DOCKER_USERNAME: ${{ secrets.DOCKER_USERNAME }}
      VPS_HOST: ${{ secrets.VPS_HOST }}
      VPS_USER: ${{ secrets.VPS_USER }}
      VPS_PASSWORD: ${{ secrets.VPS_PASSWORD }}
      VPS_PORT: ${{ secrets.VPS_PORT }}
      APP_ENV: ${{ secrets.APP_ENV }}
